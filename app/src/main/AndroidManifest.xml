<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fire.esp"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- Updated minimum and target SDK -->
    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="34"/>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/> <!-- Optional for QR/Phone verification -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/> <!-- Optional -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FireESP">

        <!-- Launcher / Main entry point -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Home activity (Bottom Navigation) -->
        <activity android:name=".activities.HomeActivity" android:exported="true"/>

        <!-- Optional standalone activities -->
        <activity android:name=".activities.TournamentActivity" android:exported="true"/>
        <activity android:name=".activities.LeaderboardActivity" android:exported="true"/>
        <activity android:name=".activities.ProfileActivity" android:exported="true"/>
        <activity android:name=".activities.AdminActivity" android:exported="true"/>

        <!-- Supabase / Google OAuth callback -->
        <activity
            android:name=".activities.OAuthCallbackActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="com.fire.esp" android:host="oauth-callback"/>
            </intent-filter>
        </activity>

        <!-- Optional splash screen -->
        <activity android:name=".activities.SplashActivity" android:exported="true"/>

    </application>
</manifest>
